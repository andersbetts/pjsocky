; Minimal config for the pjsocky live-test PBX container. Directory
; paths are deliberately omitted - the Ubuntu package's compiled-in
; defaults are correct inside the container.
[options]
verbose = 3
